services:
  postgres:
    image: postgres:16
    container_name: companies_postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: companies_db
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
